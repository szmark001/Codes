function Board(blockData, player, highLightImgArray) {
	//highLightImgArray sourced on higher level
	
	//constructor should make groups which adds Block and (diceImage inside Block)
	//constructor should also manage layering between groups
	this.selectedBlock = {};
	
	this.select(block) = function();
	this.deselect(block) = function();
	
}